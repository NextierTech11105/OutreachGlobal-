import { WatershedLanding } from "@/components/landing/watershed-landing";

export const metadata = {
  title: "NEXTIER - Water Always Finds Its Way",
  description:
    "Revenue execution engine. Like water carving through rock, NEXTIER's execution chain inevitably flows from data to deals.",
};

export default function WatershedPage() {
  return <WatershedLanding />;
}
