{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "buildCommand": "pnpm --filter @nextier/common build && pnpm --filter @nextier/dto build && cd apps/front && pnpm build",
  "devCommand": "cd apps/front && pnpm dev",
  "installCommand": "corepack enable && pnpm install",
  "framework": "nextjs",
  "outputDirectory": "apps/front/.next"
}
