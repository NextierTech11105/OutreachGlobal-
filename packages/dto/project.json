{
  "name": "dto",
  "projectType": "library",
  "root": "packages/dto",
  "sourceRoot": "packages/dto/src",
  "tags": ["type:lib", "scope:shared"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "pnpm --filter @nextier/dto build"
      }
    }
  }
}
