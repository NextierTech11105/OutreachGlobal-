{
  "name": "common",
  "projectType": "library",
  "root": "packages/common",
  "sourceRoot": "packages/common/src",
  "tags": ["type:lib", "scope:shared"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "pnpm --filter @nextier/common build"
      }
    }
  }
}
